type Query {
    users: [User]
    user(id:ID!): User,
    latestUsers: [User]
}

type User {
    id: ID!,
    name: String!,
    email: String!,
    password: String!,
}

type Mutation {
    addUser: User,
    editUser: User,
    deleteUser: Boolean
}
